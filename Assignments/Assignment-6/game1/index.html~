<html lang="te" dir="ltr">
<head>
<meta charset="utf-8">
<script src="https://ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js"></script>
<script type="text/javascript" src="/home/hasitha/Desktop/scrollmenu/jquery-1.3.1.min.js"></script>
<script type="text/javascript" src="/home/hasitha/Desktop/scrollmenu/jquery.easing.1.3.js"></script>
<script type="text/javascript" src="/home/hasitha/Desktop/scrollmenu/jquery.color.js"></script>
<style>
.container {
	max-width: 960px;
	margin: 0 auto;
	padding: 1em;
}
#beginner {
	list-style: none;
	padding: 0;
	margin: 0 0 1em;
}

#beginner li {
	display: inline-block;
	padding: 0.5em 1em;
	margin: 0 1em 1em 0;
	border: 1px solid #D9D9D9;
	background: #F7F7F7;
	border-radius: 3px;
}
#Singular,
#Plural
 {
	border: 5px dashed #D9D9D9;
	border-radius: 10px;
	padding: 5em 2em;
	text-align: center;
}
#medieval {
    width: 100px;
    height: 100px;
    background-color: #0FF;}
#expert {
    width: 100px;
    height: 100px;
    background-color: #0FF;
}
.over {
	background: #F7F7F7;
}

.clearfix {
  *zoom: 1; }

.clearfix:before,
.clearfix:after {
  display: table;
  line-height: 0;
  content: ""; }

.clearfix:after {
  clear: both; }



</style>
<script>

 $(function() {


   var people = [];
  
$.getJSON('data.json', function(data) {
$.each(data.display, function(i, f) {
          var t = "<tr>"+ "<td>" + f.title +"</td>"+"</tr>"
           
          $(t).appendTo("#title");
     });
   });
$.getJSON('data.json', function(data) {
$.each(data.language, function(i, f) {
	var sp=Math.floor(Math.random()*2);
	
	var j=0;
var strt = "<tr>"+ "<td>" 
var end="</td>"+"</tr>"
var x=strt;
	if(sp==0)
	{
	$.each(f.beginner,function(i1,f1){
	var m=Math.floor(Math.random()*f1.singular.length);
	while(j<f1.singular[m].length)
         {
	
	 x =  x + f1.singular[m][j]
         j++;
	}
         x=x+end;  
         $(x).appendTo('#beginner li');
	});
	}
	else if(sp==1)
	{
	$.each(f.beginner,function(i1,f1){
	var m=Math.floor(Math.random()*f1.plural.length);
	while(j<f1.plural[m].length)
         {
	
	 x =  x + f1.plural[m][j]
         j++;
	}
         x=x+end;  
         $(x).appendTo('#beginner li');
	});
	}
	
     });
   });
$.getJSON('data.json', function(data) {
$.each(data.language, function(i, f) {
	var m=Math.floor(Math.random()*f.medieval.length);
	var j=0;
	var strt = "<tr>"+ "<td>" 
	var end="</td>"+"</tr>"
	var x=strt;
	while(j<f.medieval[m].length)
         {
	 x =  x + f.medieval[m][j]
          j++;}
         x=x+end;  
          $(x).appendTo('#medieval');
     });
   });
$.getJSON('data.json', function(data) {
$.each(data.language, function(i, f) {
	var m=Math.floor(Math.random()*f.expert.length);
	var j=0;
 var strt = "<tr>"+ "<td>" 
var end="</td>"+"</tr>"
var x=strt;
	while(j<f.expert[m].length)
         {
	 x =  x + f.expert[m][j]
          j++;}
         x=x+end;  
          $(x).appendTo('#expert');
     });
   });
$('#medieval').hide();
$('#expert').hide();
 $('#level').on('change', function() {
   

    if (this.value=="b")
    {
        $('#beginner li').show();
	$('#medieval').hide();
	$('#expert').hide();
    }
    else if(this.value=="m")
    {
        $('#medieval').show();
	$('#beginner li').hide();
	$('#expert').hide();
    }
else if(this.value=="e")
    {  
	$('#beginner li').hide();
	$('#medieval').hide();
	$('#expert').show();
    }


});
window.onload=function() {

	var dropZoneOne = document.querySelector('#Singular');
	var dropZoneTwo = document.querySelector('#Plural');
	var dragElements = document.querySelectorAll('#beginner li');
	var elementDragged = null;

	for (var i = 0; i < dragElements.length; i++) {
		
		// Event Listener for when the drag interaction starts.
		dragElements[i].addEventListener('dragstart', function(e) {
			e.dataTransfer.effectAllowed = 'move';
			e.dataTransfer.setData('text', this.innerHTML);
			elementDragged = this;
		});

		// Event Listener for when the drag interaction finishes.
		dragElements[i].addEventListener('dragend', function(e) {
			elementDragged = null;
		});
	};
	// Event Listener for when the dragged element is over the drop zone.
	dropZoneOne.addEventListener('dragover', function(e) {
		if (e.preventDefault) {
			e.preventDefault();
		}

		e.dataTransfer.dropEffect = 'move';

		return false;
	});


	// Event Listener for when the dragged element enters the drop zone.
	dropZoneOne.addEventListener('dragenter', function(e) {
		this.className = "over";
	});

	// Event Listener for when the dragged element leaves the drop zone.
	dropZoneOne.addEventListener('dragleave', function(e) {
		this.className = "";
	});

	// Event Listener for when the dragged element dropped in the drop zone.
	dropZoneOne.addEventListener('drop', function(e) {
		if (e.preventDefault) e.preventDefault(); 
  	if (e.stopPropagation) e.stopPropagation(); 

		this.className = "";
		this.innerHTML = "Dropped " + e.dataTransfer.getData('text');
		if(typeof e.dataTransfer.getData('text') =="singular")
		{alert("yahoooo");}
		else {alert("niyankanmma"+typeof e.dataTransfer.getData('text'));}
		// Remove the element from the list.
		document.querySelector('#beginner').removeChild(elementDragged);
		elementDragged = null;
		
		return false;
	});
	// Event Listener for when the dragged element is over the drop zone.
	dropZoneTwo.addEventListener('dragover', function(e) {
		if (e.preventDefault) {
			e.preventDefault();
		}

		e.dataTransfer.dropEffect = 'move';

		return false;
	});


	// Event Listener for when the dragged element enters the drop zone.
	dropZoneTwo.addEventListener('dragenter', function(e) {
		this.className = "over";
	});

	// Event Listener for when the dragged element leaves the drop zone.
	dropZoneTwo.addEventListener('dragleave', function(e) {
		this.className = "";
	});

	// Event Listener for when the dragged element dropped in the drop zone.
	dropZoneTwo.addEventListener('drop', function(e) {
		if (e.preventDefault) e.preventDefault(); 
  	if (e.stopPropagation) e.stopPropagation(); 

		this.className = "";
		this.innerHTML = "Dropped " + e.dataTransfer.getData('text');
		
		// Remove the element from the list.
		document.querySelector('#beginner').removeChild(elementDragged);
		elementDragged = null;

		return false;
	});
}
});
//});
</script>
 <meta name="viewport" content="width=device-width, initial-scale=100">
  <link rel="stylesheet" href="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.min.js"></script>
  <script src="http://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/js/bootstrap.min.js"></script>
<script src="http://code.jquery.com/ui/1.9.2/jquery-ui.js"></script>
<script src="https://ajax.googleapis.com/ajax/libs/jqueryui/1.9.2/jquery-ui.min.js"></script>
</head>

<body>

<div class="container">
<div class="jumbotron" >
<div id="display">
</div>
 
<select id="level">
  <option value="b">Beginner</option>
  <option value="m">Medieval</option>
  <option value="e">Expert</option>
</select>
<div id="Singular">
</div>
<div id="Plural" >

</div>
<div id="dd-elements" class="clearfix">
<ul id="beginner">
<li draggable="true">hi</li>
</ul>
</div>
<div id="medieval">
hello
</div>
<div id="expert">
bonjour
</div>


</div>
</body>
</html>


